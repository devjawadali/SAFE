# Test Environment Configuration
# Copy this file to .env.test and update with your test database credentials

# Server Configuration
PORT=4000
NODE_ENV=test

# Authentication (use test-specific secrets)
JWT_SECRET=test-secret-key-for-testing-only-change-in-production
ACCESS_TOKEN_EXPIRY=30m
REFRESH_TOKEN_EXPIRY_DAYS=7
OTP_EXPIRY=300

# Database Configuration (Test Database)
# Use a separate test database to avoid polluting development data
DATABASE_URL=postgresql://postgres:password@localhost:5432/saferide_test_db
# Or use individual parameters:
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=saferide_test_db
# DB_USER=postgres
# DB_PASSWORD=password

# Security
CORS_ORIGIN=*

# Error Tracking (Optional - can be empty for tests)
SENTRY_DSN=
SENTRY_RELEASE=saferide-backend@test

# Socket.io (Optional)
SOCKET_TRANSPORTS=websocket,polling
